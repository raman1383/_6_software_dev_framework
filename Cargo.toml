[package]
name = "_6_software_dev_framework"
version = "0.1.0"
edition = "2021"
author = "Raman Shariati"
description = """A Multi-level(in difficalty & controll) framework for developing any kind of software from home to global
enterprize. with first level normal people can build their own software, leveling up gives you more controll and 
customization ability comming with more difficalty. 
One of great features of software generated by this framework is its ability to load laizily and its features get paginated
in a way that there can be many but one loads the code for that feature if user requested it spcificly, not getting all of 
them loaded only by opening the app"""

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
